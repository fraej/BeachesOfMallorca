{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "BeachesOfMallorca",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8080/index.html",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start Web Server"
        }
    ]
}